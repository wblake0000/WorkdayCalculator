# WorkdayCalculator

Simple single-file web app (`index.html`) that calculates the number of workdays between two dates.

## Rules
- Workdays are Tuesday through Saturday.
- Start/end dates are rounded to the nearest Tuesday before counting.
- Counting is inclusive.

## Usage
Open `index.html` in any modern browser.
